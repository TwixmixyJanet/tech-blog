{{!-- ALL the HTML! Here is where it all begins --}}
<html lang="en">
    <head>
        {{!-- That's so meta --}}
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        {{!-- Bootstrap CDNs --}}
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" />
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" />
        {{!-- custom stylesheets --}}
        <link rel="stylesheet" type="text/css" href="/css/jass.css">
        <link rel="stylesheet" type="text/css" href="/css/style.css">
        {{!-- Keep it fun with google fonts! --}}
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Share+Tech+Mono&display=swap" rel="stylesheet">

        <title>Janet's Tech Blog</title>
    </head>
    <body>
        <header class="navBar">
            <nav>
                {{!-- If logged in, display the following navbar options --}}
                {{#if logged_in}}
                <a href="/">Home<i class="bi bi-house"></i></a>
                <a href="/dashboard">Dashboard<i class="bi bi-speedometer"></i></a>
                <button class="no-button" id="logout">Logout<i class="bi bi-box-arrow-in-right"></i></button>
                {{else}}
                {{!-- otherwise, display these --}}
                <a href="/">Home<i class="bi bi-house"></i></a>
                <a href="/dashboard">Dashboard<i class="bi bi-speedometer"></i></a>
                <a href="/login">Login<i class="bi bi-box-arrow-in-left"></i></a>
                {{/if}}
            </nav>
        </header>
        <h1 class="text-center">Janet's Tech Blog</h1>
        <main>
            {{!-- All the website content comes in through here --}}
            {{{body}}}
        </main>
        <footer class="text-center"><a href="https://github.com/TwixmixyJanet/tech-blog" target="_blank" rel="noopener noreferrer">Learn more about this project<i class="bi bi-github"></i></a></footer>
        {{!-- No need to use the logout JS unless logged in --}}
        {{#if logged_in}}
        <script src="/js/logout.js"></script>
        {{/if}}
    </body>
</html>